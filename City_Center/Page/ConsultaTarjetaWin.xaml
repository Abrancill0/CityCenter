<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:xfx="clr-namespace:Xfx;assembly=Xfx.Controls" 
             BindingContext="{Binding Main, Source={StaticResource Locator}}" 
             x:Class="City_Center.Page.ConsultaTarjetaWin"
             BackgroundColor = "#250c4b">
    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="Chat" />
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Name="EntryStyle" TargetType="xfx:XfxEntry">
                <Setter Property="TextColor" Value="Black" />
                <Setter Property="FontFamily" Value="Roboto-Regular" />
                <Setter Property="ActivePlaceholderColor" Value="#7A5BA5" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout >
            <Label Margin="6,0,0,0" TextColor = "White" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" HorizontalTextAlignment="Start" 
                   Text="Consultá los puntos que tenés en tu tarjeta WIN" VerticalOptions = "Start" FontSize="12" FontFamily="Oswald-Regular" >
                 <Label.FontFamily>
                    <OnPlatform x:TypeArguments="x:String">
                        <On Platform="iOS" Value="Fonts/Oswald-Regular"/>
                        <On Platform="Android" Value="Oswald-Regular.ttf#Oswald-Regular"/>
                    </OnPlatform>
                </Label.FontFamily>
            </Label>
            <StackLayout>
                <Label Text="Tipo documento" TextColor="Gray" Margin="5,2,0,0" FontSize="Small" />
                <Grid Padding="15, 0">
                    <Image Source="rowdown" WidthRequest="15" HorizontalOptions="End" />
                    <Picker Margin="5" x:Name="TipoDocumento" ItemsSource="{Binding ListaOpciones}" Title="Tipo Documento">
                    </Picker>
                </Grid>
            </StackLayout>
       </StackLayout>
    </ContentPage.Content>
</ContentPage>